El archivo "User.tsx" es el componente donde se encuentran los estilos y la estructura del proyecto, mientras que el archivo "explore.tsx" es la pantalla de expo secundaria que se uso para la visualizacion
